import { a, G, x, y } from "./src/this part doesn't even matter";



// your cursor must be between code. placing the cursor at the
// end of the file or at the beginning will not trigger the
// behavior








a;
G;
y;
x;
